management:
  endpoint:
    health:
      # Developer-friendly visibility (local only)
      show-details: always

  # Optional: only relevant if a Prometheus registry is added later.
  # endpoints:
  #   web:
  #     exposure:
  #       include: health,info,metrics,prometheus